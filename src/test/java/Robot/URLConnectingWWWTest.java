/*
package Robot;

import org.testng.annotations.Test;

import java.net.URL;
import java.net.URLConnection;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;


*/
/**
 * Created by jakub on 22.03.16.
 *//*

public class URLConnectingWWWTest {

NIE MOŻNA MOCKOWAĆ KLASY FINALNEJ

    @Test
    public void ifURLConnectingIsSet() throws Exception {

        URL url = mock(URL.class);
        URLConnection expected = mock(URLConnection.class);


        when(url.openConnection()).thenReturn(expected);

        UrlConectingWWW urlConectingWWW = new UrlConectingWWW();
        URLConnection result;
        result = urlConectingWWW.explore(url);

        assertThat(result).isEqualTo(expected);

    }


}
*/
